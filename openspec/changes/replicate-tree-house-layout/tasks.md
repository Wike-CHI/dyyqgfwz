## 1. Configuration & Global Styles

- [x] 1.1 Install necessary dependencies (e.g., `clsx`, `tailwind-merge` if missing) and configure Tailwind CSS in `frontend/tailwind.config.ts` with custom colors (Deep Blue, Amber) and font families.
- [x] 1.2 Update `frontend/src/app/globals.css` to import fonts and set base styles.

## 2. Core Layout Components

- [x] 2.1 Implement `FluidGrid` component in `frontend/src/components/layout/FluidGrid.tsx` with 24-column support.
- [x] 2.2 Implement `LocationTable` component in `frontend/src/components/layout/LocationTable.tsx`.
- [x] 2.3 Implement `ProductGrid` component in `frontend/src/components/layout/ProductGrid.tsx`.
- [x] 2.4 Implement `ZigZagSection` component in `frontend/src/components/layout/ZigZagSection.tsx`.
- [x] 2.5 Implement `MultiColumnText` component in `frontend/src/components/layout/MultiColumnText.tsx`.

## 3. Page Implementation

- [x] 3.1 Create/Update Homepage at `frontend/src/app/page.tsx` using `FluidGrid`.
- [x] 3.2 Create Order Ahead page at `frontend/src/app/order-ahead/page.tsx` using `LocationTable`.
- [x] 3.3 Create Merch page at `frontend/src/app/merch/page.tsx` using `ProductGrid`.
- [x] 3.4 Create Visiting page at `frontend/src/app/visiting/page.tsx` using `ZigZagSection`.
- [x] 3.5 Create Careers page at `frontend/src/app/careers/page.tsx` using `MultiColumnText`.

## 4. Verification

- [x] 4.1 Verify that all pages render without errors and match the layout analysis structure.
