## 1. Components Refactoring

- [ ] 1.1 Update `HeroSection.tsx` to use `Poppins` font (700 weight, ~62px size) for the H1 title.
- [ ] 1.2 Update `NavBar.tsx` to increase vertical padding (`py-10` approx) and adjust logo size.
- [ ] 1.3 Create `ContentGrid.tsx` (or refactor `MasonryGrid`) to use a 4-column grid with 3:4 aspect ratio images.
- [ ] 1.4 Update `Footer.tsx` styles (background color, padding).

## 2. Page Updates

- [ ] 2.1 Update `frontend/src/app/page.tsx` to use the new `ContentGrid` component.
- [ ] 2.2 Update CTA buttons in `page.tsx` to match the polished design (cleaner styles).

## 3. Verification

- [ ] 3.1 Verify the layout matches the new specs (grid columns, header height, fonts).
