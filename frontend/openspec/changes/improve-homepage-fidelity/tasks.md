## 1. Components Implementation

- [x] 1.1 Implement `HeroSection` component in `frontend/src/components/layout/HeroSection.tsx` with full-screen video background support and overlay slots.
- [x] 1.2 Create `NavBar` component in `frontend/src/components/layout/NavBar.tsx` supporting transparent/solid states.
- [x] 1.3 Implement `MasonryGrid` component in `frontend/src/components/layout/MasonryGrid.tsx` for "What's New" content.
- [x] 1.4 Implement `Footer` component in `frontend/src/components/layout/Footer.tsx` with site links.
- [x] 1.5 Update `frontend/src/app/globals.css` to ensure full-height support and reset margins if needed.

## 2. Page Assembly

- [ ] 2.1 Refactor `frontend/src/app/page.tsx` to use the new `HeroSection` and `NavBar`.
- [ ] 2.2 Add the "What's New" section using `MasonryGrid` with mock data.
- [ ] 2.3 Add the "Tree House in Motion" video section.
- [ ] 2.4 Add the "Quote" section with styled typography.
- [ ] 2.5 Add the "Grain to Glass" feature grid section.
- [ ] 2.6 Add the `Footer` to the page.

## 3. Verification

- [ ] 3.1 Verify the homepage layout matches the visual hierarchy of the reference site.
